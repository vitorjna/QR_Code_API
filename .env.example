# Example .env file for QR_Code_API
#
# If you want to run the Flask app with SSL, provide the paths to your
# SSL certificate and key files. Otherwise, the app will run with an
# adhoc SSL context (for development/testing only).

# CERT_PATH=/path/to/your/certificate.pem
# KEY_PATH=/path/to/your/private_key.pem